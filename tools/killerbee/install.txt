# Install Prerequisites
sudo apt install git python-yaml python-pip python-gtk2 python-cairo python-usb python-crypto python-serial python-dev libgcrypt-dev
pip install rangeparser

# Install scapy
cd ~ & git clone https://github.com/secdev/scapy
cd scapy
sudo python setup.py install

# Install killerbee
cd ~
git clone https://github.com/riverloopsec/killerbee.git
cd killerbee/
sudo python setup.py install